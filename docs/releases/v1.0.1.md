## PDFQFZ CLI v1.0.1

发布日期：2025-11-06

### 更新内容
- **原地盖章**：默认直接在原始 PDF 上完成盖章，减少额外文件管理；如需输出副本，可使用 `--output` 指定目录。
- **文档更新**：README 已消除乱码，明确记录默认覆盖行为与相关参数。

### 附件
- `PDFQFZ.CLI-win-x64-1.0.1.zip`

### 升级建议
下载压缩包并覆盖旧版本即可。如需保留原文件，请在命令中显式添加 `--output <dir>`。
